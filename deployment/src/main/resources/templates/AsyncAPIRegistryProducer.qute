package {packageName};

import javax.enterprise.inject.Produces;
import io.quarkiverse.asyncapi.config.AsyncAPIRegistry;

public class AsyncAPIRegistryProducer  {
    @Produces
    AsyncAPIRegistry getAsyncAPIRegistry() {
       return AsyncAPIRegistryFactory.get();
    } 
}