package {packageName};

import io.quarkiverse.asyncapi.config.JacksonAsyncAPISupplier;
import javax.enterprise.context.ApplicationScoped;

@ApplicationScoped
public class {id}AsyncAPISupplier extends JacksonAsyncAPISupplier {

    public {id}AsyncAPISupplier () {
       super ("{id}", "{asyncAPI}");
    }
}